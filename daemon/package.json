{
  "name": "littleimpd",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --watch src/index.ts",
    "start": "bun src/index.ts",
    "check": "bun tsc --noEmit",
    "db:seed": "bun run src/db/seed/dev-seed.ts",
    "test": "bun test"
  },
  "dependencies": {
    "hono": "^4.12.7",
    "pdf-parse": "^2.4.5",
    "sqlite-vec": "^0.1.7-alpha.2"
  },
  "devDependencies": {
    "@types/bun": "^1.2"
  }
}